import 'package:flutter/material.dart';

class StackedList extends StatelessWidget {
  const StackedList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
